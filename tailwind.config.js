/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
        "templates/**/*.html",  // Adjust if your app name is different
    ],
    theme: {
        extend: {},
    },
    plugins: [],
}